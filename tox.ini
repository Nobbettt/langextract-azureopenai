# Copyright 2025 Google LLC.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

[tox]
envlist = py310, py311, py312, py313, lint, mypy, format-check
skip_missing_interpreters = True

[testenv]
description = Run unit tests
setenv =
    PYTHONWARNINGS = ignore
    # Disable API calls in tests unless explicitly enabled
    AZURE_OPENAI_API_KEY = test-key
    AZURE_OPENAI_ENDPOINT = https://test.openai.azure.com/
extras = 
    test
deps =
    pytest>=7.0
    pytest-mock>=3.10.0
    pytest-asyncio>=0.21.0
commands =
    pytest tests/ -v --tb=short {posargs}

[testenv:lint]
description = Run linting with ruff
skip_install = false
extras = dev
deps =
    ruff>=0.1.0
commands =
    ruff check langextract_azureopenai/ tests/ {posargs}

[testenv:mypy]
description = Run type checking with mypy
skip_install = false
extras = dev
deps =
    mypy>=1.0.0
    types-requests
commands =
    mypy langextract_azureopenai/ {posargs}

[testenv:format]
description = Run code formatting
skip_install = true
deps =
    black>=22.0.0
    isort>=5.0.0
    ruff>=0.1.0
commands =
    isort langextract_azureopenai/ tests/
    black langextract_azureopenai/ tests/
    ruff check --fix langextract_azureopenai/ tests/

[testenv:format-check]
description = Check code formatting
skip_install = true
deps =
    black>=22.0.0
    isort>=5.0.0
    ruff>=0.1.0
commands =
    isort --check-only --diff langextract_azureopenai/ tests/
    black --check --diff langextract_azureopenai/ tests/
    ruff check langextract_azureopenai/ tests/

[testenv:integration]
description = Run integration tests (requires real Azure OpenAI credentials)
setenv =
    # Integration tests use real API credentials
    PYTHONWARNINGS = ignore
extras = 
    test
deps =
    pytest>=7.0
    pytest-mock>=3.10.0
    pytest-asyncio>=0.21.0
passenv =
    AZURE_OPENAI_API_KEY
    AZURE_OPENAI_ENDPOINT
commands =
    pytest tests/ -v --tb=short -m integration {posargs}

[testenv:coverage]
description = Run tests with coverage
extras = 
    test
deps =
    pytest>=7.0
    pytest-cov>=4.0.0
    pytest-mock>=3.10.0
    pytest-asyncio>=0.21.0
commands =
    pytest tests/ --cov=langextract_azureopenai --cov-report=term-missing --cov-report=html {posargs}

[testenv:docs]
description = Build documentation
extras = docs
commands =
    mkdocs build

[testenv:clean]
description = Clean up temporary files
skip_install = true
deps =
commands =
    python -c "import shutil; shutil.rmtree('dist', ignore_errors=True)"
    python -c "import shutil; shutil.rmtree('.tox', ignore_errors=True)"
    python -c "import shutil; shutil.rmtree('htmlcov', ignore_errors=True)"
    python -c "import shutil; shutil.rmtree('.pytest_cache', ignore_errors=True)"
    python -c "import shutil; shutil.rmtree('.mypy_cache', ignore_errors=True)"
    python -c "import shutil; shutil.rmtree('__pycache__', ignore_errors=True)"